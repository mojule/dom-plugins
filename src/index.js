'use strict'

const api = require( './plugins/api' )
const core = require( './plugins/core' )
const privates = require( './plugins/privates' )
const statics = require( './plugins/statics' )

module.exports = { api, core, privates, statics }
